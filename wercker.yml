# Configuration file for http://wercker.com
box: timnn/texlive

build:
    steps:
        - install-packages:
            name: 'install curl'
            packages: 'curl'
        - script:
            name: 'init and setup submodule'
            code: 'ci-scripts/init.sh'
        - script:
            name: 'testing with ta9boh'
            code: 'ci-scripts/test.sh'
        - script:
            name: 'building a pdf'
            code: 'ci-scripts/build.sh'

deploy:
    steps:
        - install-packages:
            name: 'install curl'
            packages: 'curl'
        - script:
            name: 'deploy to slack'
            code: 'ci-scripts/deploy-slack.sh'
